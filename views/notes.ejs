<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="preload" href="https://assets.vercel.com/raw/upload/v1587415301/fonts/2/inter-var-latin.woff2" as="font" type="font/woff2" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/notes.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css" integrity="sha512-+4zCK9k+qNFUR5X+cKL9EIR+ZOhtIloNl9GIKS57V1MyNsYpYcUrUeQc9vNfzsWfV28IaLL3i96P9sdNyeRssA==" crossorigin="anonymous" />
    <title>edunote</title>
</head>
<body>
    <nav class="navbar">
        <h1>edunote</h1>
        <a class="nav-item" href="/ask" style="margin-left: 800px;">ask</a>
        <a class="nav-item" href="/notes" id="active">notes</a>
        <a class="nav-item" href="/recordings">recordings</a>
        <a class="nav-item" href="/profile" style="margin-right: 100px;">profile</a>
    </nav>
    <div class="notes">
        <h1>Notes</h1>
        <form method="POST" action="/add-note">
            <div class="inline">
                    <input required placeholder="note content" name="noteContent">
                    <button type="submit"><i class="fas fa-plus-square"></i>
            </div>
        </form>
        <div class="note-grid">
            <div class="inline">
                <% if(notes.length == 0) { %> 
                    <div class="note-card">
                        <p>no notes</p>
                    </div>
                <% } %>
                <% for (let i = 0; i < notes.length; i++) { %> 
                    <div class="note-card">
                        <form method="POST" action="/delete-note">
                            <input type="hidden" value="<%- notes[i]._id %>" name="noteId">
                            <button type="submit">
                                <i id="delete-note" class="fa fa-trash-alt"></i>
                            </button>
                        </form>
                        <p><%- notes[i].content %></p>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>